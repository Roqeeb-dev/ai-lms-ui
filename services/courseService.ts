export const course = {
  // create course - must be authenticated as instructor or admin
  // update course - must be authenticated as instructor or admin
  // delete course - must be authenticated as instructor or admin
  // get courses - any user
  // get specific course details - any user
  // get logged in instructor courses - must be authenticated as instructor or admin
  // get an instructor course - any user
};
